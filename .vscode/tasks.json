{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Aurora Cafe (JDK 26)",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-Command",
        "New-Item -ItemType Directory -Force out | Out-Null; $files = Get-ChildItem -Recurse src -Filter *.java | ForEach-Object { $_.FullName }; javac --release 26 -encoding UTF-8 -d out $files"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$javac"
    }
  ]
}
